{
  "name": "beyond-audit-website",
  "version": "1.0.0",
  "description": "Beyond Audit - Professional accounting services website with Node.js and React",
  "main": "server.js",
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=9.0.0"
  },
  "scripts": {
    "start": "node backend/server.js",
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "server": "nodemon backend/server.js",
    "client": "cd frontend && npm start",
    "build": "cd frontend && npm install && CI=false npm run build",
    "install-all": "npm install && cd frontend && npm install",
    "vercel-build": "cd frontend && npm install && CI=false npm run build",
    "postinstall": "cd frontend && npm install"
  },
  "keywords": ["accounting", "audit", "tax", "financial", "beyond-audit"],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "body-parser": "^1.20.2",
    "nodemailer": "^6.9.7"
  },
  "devDependencies": {
    "nodemon": "^3.0.1",
    "concurrently": "^8.2.2"
  }
}

